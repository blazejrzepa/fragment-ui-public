# Hoist Storybook dependencies to root for better module resolution
# This is critical for Storybook to work in pnpm monorepo
public-hoist-pattern[]=*storybook*
public-hoist-pattern[]=*vite*
public-hoist-pattern[]=@storybook/*
public-hoist-pattern[]=storybook
public-hoist-pattern[]=@storybook/addon-*
public-hoist-pattern[]=@storybook/react*

# Shamefully hoist to fix deep dependency resolution issues  
# Note: This is REQUIRED for Storybook to work in pnpm monorepo
# Storybook addons use deep imports that pnpm cannot resolve correctly
shamefully-hoist=true
strict-peer-dependencies=false

# Use hoisted node linker for better compatibility with Storybook
# This creates a flat node_modules structure (like npm/yarn)
# node-linker=hoisted

